defaults: &DEFAULTS

development:
  <<: *DEFAULTS
  secret_key_base: <%= SecureRandom.hex(64) %>

staging:
  <<: *DEFAULTS
  secret_key_base: <%= SecureRandom.hex(64) %>

production:
  <<: *DEFAULTS
  secret_key_base: <%%= ENV["SECRET_KEY_BASE"] %>

test:
  <<: *DEFAULTS
  secret_key_base: <%= SecureRandom.hex(64) %>